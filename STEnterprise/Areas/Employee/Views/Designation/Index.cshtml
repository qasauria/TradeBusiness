
@using GridMvc.Html
@model IEnumerable<STEnterprise.Areas.Employee.Models.Designation>


@{
    ViewBag.Title = "Designation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    span.field-validation-error {
        color: red;
    }
</style>

<div class="code-cut">
    <a class="popup btn btn-primary" href="/Employee/Designation/Save/0" style="margin-bottom:20px; margin-top:20px;">Add New Designation</a>
    @Html.Grid(Model).Columns(columns =>
{
    columns.Add(a => a.DesignationName).Titled("Designation Name").Sortable(true).Filterable(true);
    columns.Add(a => a.UserStatus).Titled("Status").Sortable(true).Filterable(true);
    columns.Add()
    .Encoded(false)
    .Sanitized(false)
    .SetWidth(30)
    .RenderValueAs(a => Html.ActionLink("Edit", "Edit", new { id = a.DesignationId, @class = "btn btn-primary" })).Titled("<span>Edit</span>");
    columns.Add()
   .Encoded(false)
   .Sanitized(false)
   .SetWidth(30)
   .RenderValueAs(a => Html.ActionLink("Delete", "Delete", new { id = a.DesignationId })).Titled("Delete");
}).WithPaging(10).Sortable(true)

</div>


